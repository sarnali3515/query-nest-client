import { Link } from "react-router-dom";

const MyQueries = () => {
    return (
        <div>
            <Link to="/add-queries">Add Queries</Link>
        </div>
    );
};

export default MyQueries;